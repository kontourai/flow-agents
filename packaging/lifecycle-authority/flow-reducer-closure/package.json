{
  "name": "flow-reducer-closure",
  "version": "1.0.0",
  "private": true,
  "description": "Lockfile-bearing manifest for the audited Flow reducer closure (#1054). The lifecycle-authority eval installs this exact dependency graph in isolation; the verifier binds paths, bytes, symlink targets, and executable classification while the privileged installer canonicalizes ambient umask differences. Regenerate the lock in an isolated temporary directory and verify the complete installed tree before updating the independent digest.",
  "dependencies": {
    "@kontourai/flow": "3.12.0"
  },
  "overrides": {
    "@kontourai/surface": "2.12.0"
  }
}
