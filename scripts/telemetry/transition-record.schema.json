{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://kontourai.dev/schemas/kontour.flow-agents.transition/1.0.json",
  "title": "kontour.flow-agents.transition",
  "description": "One record per flow-agents CLI invocation, appended to .flow-agents/telemetry/transitions.jsonl (NOT under .kontourai/, which is the run's artifact state: two evals assert a read-only or rejected command leaves that tree byte-identical, and a per-invocation record inside it breaks both) by src/transition-log.ts at the point src/cli.ts exits (flow-agents #1274). It exists because neither existing mechanism can answer 'what did this CLI do': scripts/hooks/evidence-capture.js only runs where the kit is installed into a hook-firing provider, and both it and the host transcript read the exit status from the host tool payload, which reports 0 for any transition wrapped in a script that captures $? and continues. SCOPE, stated rather than assumed: this covers src/cli.ts only. The flow-agents-workflow-sidecar and flow-agents-validate-artifacts binaries have their own entry points, and init exits directly when an operator interrupts a prompt; those invocations emit no record. REDACTION: argv is not recorded. Only the command (validated against the registry), the verb (lowercase letters and hyphens), flag NAMES (validated as flag names), and the values of an allowlist of flags whose values are kit-defined identifiers matching an identifier pattern. Anything declined is recorded as a placeholder carrying no content. error_name is the thrown error's CLASS, never its message, because several verbs interpolate raw operator input into what they throw. The log is written at the SHARED repository root, so every linked worktree appends to one file; consumers must filter by actor.session_id or they will read another lane's run.",
  "type": "object",
  "additionalProperties": true,
  "required": ["schema", "version", "command", "verb", "targets", "flags", "exit_code", "outcome", "started_at", "duration_ms", "actor"],
  "properties": {
    "schema": { "const": "kontour.flow-agents.transition" },
    "version": { "const": "1.0" },
    "command": {
      "type": ["string", "null"],
      "description": "The registered command name, «unknown» when the first argument names no registered command, or null when none was given. Never the raw argument: a misplaced shell variable arrives there verbatim."
    },
    "verb": { "type": ["string", "null"], "description": "The first positional after the command, or «unparsed» when it is not verb-shaped." },
    "targets": {
      "type": "object",
      "additionalProperties": { "type": "string" },
      "description": "Values of allowlisted identifier flags. `flow` is derived from run state where the command resolves one (noteActiveFlow), because workflow evidence does not require the operator to pass it and expectation ids are unique only within a flow."
    },
    "flags": { "type": "array", "items": { "type": "string" }, "description": "Flag NAMES only. Never values." },
    "exit_code": { "type": "integer" },
    "outcome": {
      "enum": ["ok", "nonzero", "unhandled-error", "usage"],
      "description": "Named for what the exit code observably is, not what it is assumed to mean. 70 is this CLI's top-level catch and mixes contract refusals with genuine faults, because every refusal in the workflow verb throws; it is 'unhandled-error', not 'crashed'. A typed refusal with its own code is the real fix (#1273)."
    },
    "error_name": { "type": "string", "maxLength": 80, "description": "The thrown error's constructor name. A weak discriminator: most throws are a bare Error." },
    "started_at": { "type": "string", "format": "date-time" },
    "duration_ms": { "type": "integer", "minimum": 0, "description": "Times run() only; the record's own write is not included." },
    "cwd_repo": { "type": ["string", "null"], "description": "The shared repository root the log is anchored on." },
    "actor": {
      "type": "object",
      "required": ["runtime", "session_id"],
      "properties": {
        "runtime": { "type": ["string", "null"] },
        "session_id": { "type": ["string", "null"], "description": "Absent on runtimes that do not publish one, in which case consumers cannot separate this lane from a sibling's." }
      }
    },
    "output_tokens": {
      "type": "integer",
      "minimum": 0,
      "description": "OUTPUT TOKENS ONLY, AND A FLOOR — never a total and never currency. Written by scripts/telemetry/enrich-transitions.mjs AFTER the fact, never by the emitter (flow-agents #1320): 'at most one transition per turn' is a decision about a set of competing transitions, which a process exiting cannot make, and the emitter is deliberately independent of the host transcript this is derived from. Present IFF token_attribution.attributed is true. It under-attributes by construction in two independent ways: output tokens are roughly 6.6% of spend on this repo's own corpus and are not proportional to it (cache READ dominates and is invisible here), and a turn is consumed by at most one transition, so a transition sharing its invoking turn with an earlier one carries no number at all. An ABSENT field is NOT zero tokens — it means no turn was attributed, and a consumer must count it as unattributable (Console's transitions_without_turn) rather than folding it in as an attributed zero, which is indistinguishable from a genuinely free gate."
    },
    "token_attribution": {
      "type": "object",
      "additionalProperties": true,
      "required": ["granularity", "attributed"],
      "description": "The granularity label travelling WITH the number, mirroring the EconomicsDelegationCostGranularity = 'model-proxy' precedent, plus a POSITIVE statement for a transition that has no tokens. That positive statement is the point: a missing output_tokens alone cannot distinguish 'examined, no turn to charge' from 'nothing ever looked at this log', and conflating them reports an un-enriched run as a fully measured one.",
      "properties": {
        "granularity": { "const": "output-tokens-only", "description": "Names what the figure is rather than implying precision. Matches Console's GateCostAttribution.granularity." },
        "attributed": { "type": "boolean", "description": "true iff a host turn was found and charged; output_tokens is present iff this is true." },
        "turn_ref": { "type": "string", "description": "Path-free, session-scoped hash of the turn. Deliberately not the internal turn id, which embeds an absolute transcript path. Lets a later enrichment pass honour a turn already charged, so one-turn-one-transition holds across runs and not merely within one." },
        "reason": {
          "type": "string",
          "description": "Why nothing was attributed: no-turn-before-transition, turn-outside-grace-window, turn-consumed-by-earlier-transition, turn-claimed-by-simultaneous-transition, unparseable-started-at."
        },
        "ambiguous": { "type": "boolean", "description": "Two or more transitions share this instant and competed for the same turn; which one was charged followed line order, not the data. Surfaced rather than picked silently." },
        "source": { "type": "string", "description": "What the attribution was derived from. Names the derivation, not the machine." }
      }
    },
    "resolved_model": {
      "type": "string",
      "minLength": 1,
      "maxLength": 128,
      "description": "THE MODEL THAT EMITTED THE INVOKING TOOL CALL — not 'the model that ran the gate' (the gate is a subprocess; no model runs inside it) and not 'the session's model', which changes mid-session and differs between an orchestrator and its delegates. Written by scripts/telemetry/enrich-transitions.mjs AFTER the fact, never by the emitter (flow-agents #1327): no environment variable names the executing model on either runtime, so a field filled at emit time would be a label nothing computes. Named for the economics vocabulary (scripts/telemetry/economics-record.sh's per-delegation `resolved_model`) so the two records join. Present IFF model_attribution.attributed is true. AN ABSENT FIELD IS NOT A DEFAULT MODEL — it means the model was not observable, and a consumer must count it as unknown rather than pooling the transition into whichever models the window happened to contain. Per-gate effectiveness is plausibly model-dependent, so a pooled verdict removes a gate for every model at once on evidence that covers only some."
    },
    "model_attribution": {
      "type": "object",
      "additionalProperties": true,
      "required": ["granularity", "attributed"],
      "description": "The granularity label travelling WITH the model, plus a POSITIVE statement for a transition whose model could not be observed — the same discipline token_attribution applies to the number. Three states must stay distinguishable: attributed (a model was observed), attributed:false (examined, none observable), and NO BLOCK AT ALL (nothing ever looked — a log enriched before #1327, or not at all). Collapsing the last two reports an un-enriched log as a measured one whose gates all ran on nothing.",
      "properties": {
        "granularity": { "const": "invoking-turn-model", "description": "Names the derivation rather than implying precision, mirroring the EconomicsDelegationCostGranularity = 'model-proxy' precedent." },
        "attributed": { "type": "boolean", "description": "true iff a turn was identified within the grace window and it named a usable model; resolved_model is present iff this is true." },
        "turn_ref": { "type": "string", "description": "Path-free, session-scoped hash of the turn the model was read from. The same handle token_attribution uses, so the two can be checked against each other." },
        "reason": {
          "type": "string",
          "description": "Why nothing was attributed: no-turn-before-transition, turn-outside-grace-window, unparseable-started-at, turn-reports-no-model. The last covers a host placeholder such as `<synthetic>` — which Claude Code writes for its own generated messages (rate-limit notices, 'No response requested', a rejected model id) — and an absent field after a transcript format change. A placeholder is refused rather than folded as if it were a model."
        },
        "source": { "type": "string", "description": "What the attribution was derived from. Names the derivation, not the machine." }
      }
    }
  }
}
